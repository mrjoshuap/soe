---

galaxy_info:
  author: Joshua Preston
  description: >
    This applies a standard operating environment for MrJoshuaP Labs.
  company: MrJoshuaP Labs

  # If the issue tracker for your role is not on github, uncomment the
  # next line and provide a value
  # issue_tracker_url: http://example.com/issue/tracker

  license: GPL-3.0-only

  min_ansible_version: 2.9

  #
  # List of supported platforms, and for each platform a list of versions.
  # To view available platforms and versions (or releases), visit:
  # https://galaxy.ansible.com/api/v1/platforms/
  #
  platforms:
    - name: Fedora
      versions: [ 31, 32 ]
    - name: EL
      versions: [ 6, 7, 8 ]

  # List tags for your role here, one per line. A tag is a keyword that
  # describes and categorizes the role. Users find roles by searching for
  # tags. Be sure to remove the '[]' below, if you add tags to this list.
  #
  # NOTE: A tag is limited to a single word comprised of alphanumeric
  #       characters.  Maximum 20 tags per role.
  galaxy_tags: []

dependencies:
  - geerlingguy.nfs
  - linux-system-roles.cockpit
  - linux-system-roles.firewall
  - linux-system-roles.kdump
  - linux-system-roles.network
  - linux-system-roles.postfix
  - linux-system-roles.selinux
  - linux-system-roles.storage
  - linux-system-roles.timesync
  - linux-system-roles.tuned
  - RedHatInsights.insights-client
